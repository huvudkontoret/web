# Agent notes — web

Part of the huvudkontoret workspace. The workspace CLI is `hk`:

- `hk context web` — project brief and how to run things
- `hk status web` — current state
- `hk dev|build|test web` — run via the workspace harness

Machine-readable output: add `--json` to any hk command.

Write everything in English — commits, code, comments, docs, PRs and issues —
whatever language the surrounding history happens to use. Product content
(user-facing strings, domain grammars) is exempt and stays as the product
needs it.
